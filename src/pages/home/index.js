import React from "react";
import { NavLink, Outlet, useSearchParams, useLocation } from "react-router-dom";

const Home = () => {
	return (
		<div
			className="brightness-40 bg-center bg-no-repeat bg-cover h-screen w-screen"
			style={{ backgroundImage: "url(/images/hobbies.jpg)" }}>


				
			</div>
	);
};

export default Home;
